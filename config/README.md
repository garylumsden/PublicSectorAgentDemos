# Demo configuration

Store non-secret demo configuration in this folder.

Use the `ground` and `hosted` folders for stage-specific settings.

Use the `presenter` folder for the committed session catalog.

Act and Cross-Government Coordinate keep their configuration inside their bundled source folders.
Patriots and Tokens and Credits keep their configuration in their optional external repositories.
